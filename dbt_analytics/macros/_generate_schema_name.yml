version: 2

macros:
  - name: generate_schema_name
    description: "{{ doc('dsc_generate_schema_name') }}"
    arguments:
      - name: custom_schema_name
        description: "{{ doc('dsc_custom_schema_name') }}"
      - name: node
        description: "{{ doc('dsc_node') }}"
