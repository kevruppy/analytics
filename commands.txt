### bash

# files in cwd not having file extensions ".sock", ".log" or ".js"
find . -type f ! -name "*.sock" ! -name "*.log" ! -name "*.js"

### dbt

# run macro
dbt run-operation check_for_comments_on_cols

### linting

# run pre-commit
pre-commit run --all-files

# run sqlfluff
sqlfluff lint
